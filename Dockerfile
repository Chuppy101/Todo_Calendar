# Использую официальный Node.js образ в качестве базового
FROM node:14

# Устанавливаю рабочую директорию внутри контейнера
WORKDIR /usr/src/app

# Копирую package.json и package-lock.json
COPY package*.json ./

# Устанавливаю зависимости
RUN npm install

# Копирую весь проект в контейнер
COPY . .

# Указываю порт, который будет использован приложением
EXPOSE 3000

# Команда для запуска приложения
CMD ["npm", "start"]